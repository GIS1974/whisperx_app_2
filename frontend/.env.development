# Frontend Environment Variables (Vite specific - prefix with VITE_)
# These are embedded into the build, so no secrets here.
# For runtime config, consider fetching from a /config endpoint or using window object.

VITE_APP_TITLE="WhisperX ESL App (Dev)"
VITE_API_BASE_URL="http://localhost:5000/api"
